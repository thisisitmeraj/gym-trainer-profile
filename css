body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

/* Header */
header {
    text-align: center;
    background-color: orange;
    padding: 20px;
}
header img {
    border-radius: 50%;
}

/* Sections */
section {
    padding: 20px;
}
h2 {
    color: darkorange;
}

/* Table */
table {
    width: 100%;
    border-collapse: collapse;
}
th {
    background-color: orange;
    color: white;
}

/* Footer */
footer {
    background-color: #333;
    color: white;
    text-align: center;
    padding: 10px;
}
